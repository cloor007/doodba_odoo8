# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    #origin: https://github.com/odoo/odoo.git
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/odoo_dev.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
./ecua_contabilidad:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/ecua_contabilidad.git
  target: origin $ODOO_VERSION
  merges:
    - origin 3572ebcec0c0fa60b4d7887690af6a9f8d53a43a
    - origin 8.0-jasper

./ecuadorenlinea:
  defaults:
    depth: $DEPTH_DEFAULT
  remotes:
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/ecuadorenlinea80.git
  target: origin $ODOO_VERSION
  merges:
    - origin 31929be0a81adc9e0436aef5427550b36e46ee35

./oca_modules:
  defaults:
    depth: $DEPTH_DEFAULT
  remotes:
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/oca_modules.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

./aldelo_conector:
  defaults:
    depth: $DEPTH_DEFAULT
  remotes:
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/aldelo_conector.git
  target: origin $ODOO_VERSION
  merges:
    - origin 4b7325b0b5a293422fb73bb7a66470a8f267be99
./aldelo_pedidos_web:
  defaults:
    depth: $DEPTH_DEFAULT
  remotes:
    origin: https://oauth2:$GITLAB_TOKEN@gitlab.com/trilambda/aldelo_pedidos_web.git
  target: origin $ODOO_VERSION
  merges:
    - origin 19b36897cbd25cb90dea55a83a2a6e4e1c701c69
